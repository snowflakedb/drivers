queries:
- index: 0
  original: |-
    SELECT ROWKIND, INTVAL, VARCHARVAL, DATEVAL
    FROM ODBCMETADATATESTDB.DATATYPETESTS.ALLDATATYPES
    WHERE ROWKIND = ?
      AND INTVAL >= ?
    ORDER BY ROWKIND;
  mapped: |-
    SELECT ROWKIND, INTVAL, VARCHARVAL, DATEVAL
    FROM ODBCMETADATATESTDB.DATATYPETESTS.ALLDATATYPES
    WHERE ROWKIND = ?
      AND INTVAL >= ?
    ORDER BY ROWKIND;
