queries:
- index: 0
  original: |-
    INSERT INTO ODBCSCRATCHTESTDB.SCRATCHTESTS.SCRATCHINSERT (LABEL, NUMVAL)
    VALUES ('ado-6 marker', 42);
  mapped: |-
    INSERT INTO ODBCSCRATCHTESTDB.SCRATCHTESTS.SCRATCHINSERT (LABEL, NUMVAL)
    VALUES ('ado-6 marker', 42);
- index: 1
  original: CREATE TABLE ODBCSCRATCHTESTDB.SCRATCHTESTS.ADO6_TMP (ID INTEGER, NAME VARCHAR(64))
  # test.cpp appends _<pid> at runtime so concurrent CI runs do not collide.
  mapped: CREATE TABLE ODBCSCRATCHTESTDB.SCRATCHTESTS.ADO6_TMP (ID INTEGER, NAME VARCHAR(64))
- index: 2
  original: DROP TABLE ODBCSCRATCHTESTDB.SCRATCHTESTS.ADO6_TMP
  # test.cpp appends _<pid> at runtime so concurrent CI runs do not collide.
  mapped: DROP TABLE ODBCSCRATCHTESTDB.SCRATCHTESTS.ADO6_TMP
- index: 3
  original: SELECT COUNT(*) FROM ODBCSCRATCHTESTDB.SCRATCHTESTS.SCRATCHINSERT
  mapped: SELECT COUNT(*) FROM ODBCSCRATCHTESTDB.SCRATCHTESTS.SCRATCHINSERT
