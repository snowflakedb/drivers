# Breaking changes

## 1: Native error code should be 0 when error originates from client side

## 2: Compression type is now returned in uppercase

## 3: Encryption field is no longer included in the result

## 4: GET rowset metadata contains file size after decryption instead of file size before decryption
