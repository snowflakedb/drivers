# Numeric NumPy-mode type tests (NUMBER, REAL, DECFLOAT) and DATE numpy
# (datetime64[D]) run on the PyO3 native-arrow row path. TIMESTAMP_NTZ /
# INTERVAL stay on the default Python types until their numpy converters land.
