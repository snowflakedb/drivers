# Numeric NumPy-mode type tests (NUMBER, REAL, DECFLOAT) run on the PyO3
# native-arrow row path. DATE / TIMESTAMP_NTZ / INTERVAL stay on the default
# Python types until their numpy converters land.
