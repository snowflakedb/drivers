# Numeric NumPy-mode type tests (NUMBER, REAL, DECFLOAT), DATE numpy
# (datetime64[D]), and TIMESTAMP_NTZ numpy (datetime64[ns]) run on the PyO3
# native-arrow row path. INTERVAL stays on the default Python types until its
# numpy converter lands.
