---
name: run-jdbc-ud-tests
description: Runbook for building and running JDBC Universal Driver (UD) tests — Rust jdbc_bridge compilation, CORE_PATH setup, Gradle test commands.
# Pointer to .claude/skills/run-jdbc-ud-tests/SKILL.md (canonical source).
# Skills fire on demand — when invoked the agent reads the canonical file fresh, so
# a pointer here is safe. This avoids duplicating content.
# WHY alwaysApply rules cannot use the same approach: see .cursor/rules/*.mdc frontmatter.
---

The full skill definition is in `.claude/skills/run-jdbc-ud-tests/SKILL.md`.
