---
name: bootstrap-skills-evals
description: Bootstraps a repo for the sf ai skills convention. Invoked when the user says "set up sf ai skills", "bootstrap skills", or "run sf ai skills repo-setup".
# Pointer to .claude/skills/bootstrap-skills-evals/SKILL.md (canonical source).
# Skills fire on demand — when invoked the agent reads the canonical file fresh, so
# a pointer here is safe. This avoids duplicating content.
# WHY alwaysApply rules cannot use the same approach: see .cursor/rules/*.mdc frontmatter.
---

The full skill definition is in `.claude/skills/bootstrap-skills-evals/SKILL.md`.
