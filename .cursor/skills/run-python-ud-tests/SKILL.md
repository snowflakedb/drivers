---
name: run-python-ud-tests
description: Runbook for building and running Python Universal Driver (UD) tests — Rust core compilation, hatch environments, unit/integ/e2e commands, and adhoc script pattern.
# Pointer to .claude/skills/run-python-ud-tests/SKILL.md (canonical source).
# Skills fire on demand — when invoked the agent reads the canonical file fresh, so
# a pointer here is safe. This avoids duplicating content.
# WHY alwaysApply rules cannot use the same approach: see .cursor/rules/*.mdc frontmatter.
---

The full skill definition is in `.claude/skills/run-python-ud-tests/SKILL.md`.
