# Proto Generator

A rust tool for generating user protobuf interface files in the language of choice

## Usage

```
cargo run --bin proto_generator -- --generator <generator_name> -i <path_to_proto> -o <output_dir>
```