# Drivers Operations Directory

A duty on a live system that needs human approval before it takes effect, plus the recurring driver reports. Reading a system without changing it belongs in [../tools/README.md](../tools/README.md).

## Catalog

| Operation | Reference | When to load |
|---|---|---|
| ODBC weekly report | [odbc-weekly-report.md](odbc-weekly-report.md) | Producing the weekly ODBC progress report: `generate_weekly_report.sh`, the `--skip-reference` fallback when the Docker reference-coverage step fails, and the figures to highlight. |
