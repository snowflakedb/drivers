# Local and CI Gradle defaults for the JDBC module.
# Parallel project execution helps decorator-processor + root compile;
# the build cache reuses task outputs across clean checkouts when inputs match.
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.workers.max=8
